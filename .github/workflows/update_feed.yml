name: Update AnimeAI Feed

on:
  schedule:
    - cron: '0 */6 * * *'  # Runs every 6 hours
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HOSTNAME: ${{ secrets.HOSTNAME }}
      FEED_URI: ${{ secrets.FEED_URI }}
      BSKY_APP_USERNAME: ${{ secrets.BSKY_APP_USERNAME }}
      BSKY_APP_PASSWORD: ${{ secrets.BSKY_APP_PASSWORD }}

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔮 Generate feed and reject log
        run: python -m scripts.generate_feed

      - name: 🚧 Check if feed.json changed
        id: check_diff1
        run: |
          git diff --exit-code feed.json || echo "has_changes=true" >> $GITHUB_ENV

      - name: 🚧 Check if rejected_debug.json changed
        id: check_diff2
        run: |
          git diff --exit-code rejected_debug.json || echo "has_changes=true" >> $GITHUB_ENV

      - name: 📋 Log when feed.json is unchanged
        if: env.has_changes != 'true'
        run: echo "🧘 feed.json/rejected_debug.json unchanged — skipping commit this cycle"

      - name: 🤖 Commit & Push feed and rejected log
        if: env.has_changes == 'true'
        run: |
          git config user.name "AnimeAI Bot"
          git config user.email "bot@trevorion.io"
          git add feed.json rejected_debug.json
          git commit -m "🔄 Auto-update feed + reject log"
          git push https://x-access-token:${{ secrets.GH_PAT }}@github.com/PhantomBlackOP/AnimeAI.git HEAD:main
